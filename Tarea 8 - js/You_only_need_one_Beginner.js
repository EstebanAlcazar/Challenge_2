//Se le dará una matriz ay un valor x. Todo lo que necesitas hacer es verificar si la matriz proporcionada contiene el valor.
//La matriz puede contener números o cadenas. X puede ser cualquiera de las dos cosas.
//Devuelve truesi la matriz contiene el valor, falseen caso contrario.

function check(a, x) {
    return a.includes(x);
  }
  